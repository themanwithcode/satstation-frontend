export type EtchingRuneResponse = {
  commitAddress: string;
  minFundValue: number;
};

export type Rune = {
  ticker: string;
  total: string;
  minted: string;
  price: string;
  divisibility: string;
};
